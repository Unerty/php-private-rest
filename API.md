./leagues - returns list of `league_id`

./leagues/{league_id} - returns the name of the league

./leagues?start_timestamp={timestamp} - returns `league_ids` of the leagues happened (or still happening) after the timestamp